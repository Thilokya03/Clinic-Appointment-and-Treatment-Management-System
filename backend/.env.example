# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Raahil@123
DB_NAME=catms 

# JWT Secret (change this to a random secure string)
JWT_SECRET="your_jwt_secret_key_here_change_this_to_random_string"

# Server Port (optional, defaults to 3000)
PORT=3000
